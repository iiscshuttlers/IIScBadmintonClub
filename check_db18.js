import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env.local', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1]; const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]; const supabase = createClient(url, key); async function test() { const playerId = '1a0b56fa-25ff-4b50-b9b1-0867f07d1b8b'; const sureshId = '59c8707c-4873-46e6-a174-9192691598e0'; const tFilter = \player1_id.eq.\,player2_id.eq.\\; const { data: tData } = await supabase.from('tournament_matches').select('*').or(tFilter); const sureshMatches = tData.filter(m => m.player1_id === sureshId || m.player2_id === sureshId || m.player3_id === sureshId || m.player4_id === sureshId); console.log(sureshMatches); } test();
