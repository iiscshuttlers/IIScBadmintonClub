import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env.local', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1]; const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]; const supabase = createClient(url, key); async function test() { const { data } = await supabase.from('tournament_matches').select('id, category, player1_id, player2_id, player3_id, player4_id').eq('category', 'Singles').not('player3_id', 'is', null); console.log(data); } test();
