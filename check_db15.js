import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env.local', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1]; const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]; const supabase = createClient(url, key); async function test() { const { data } = await supabase.from('players').select('id, full_name').ilike('full_name', '%Suresh%'); console.log(data); const { data: d2 } = await supabase.from('players').select('id, full_name').ilike('full_name', '%Kumar%'); console.log(d2); } test();
