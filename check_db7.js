import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env.local', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1]; const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]; const supabase = createClient(url, key); async function test() { const id = '1a0b56fa-25ff-4b50-b9b1-0867f07d1b8b'; const { data, error } = await supabase.from('players').select('id, elo_rating, singles_elo, doubles_elo, mixed_elo, gender').is('deleted_at', null); const sortedOverall = [...data].sort((a, b) => (b.elo_rating || 0) - (a.elo_rating || 0)); const overallRank = sortedOverall.findIndex((p) => p.id.toLowerCase() === id.toLowerCase()) + 1; console.log(overallRank); } test();
