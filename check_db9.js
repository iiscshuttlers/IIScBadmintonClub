import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env.local', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1]; const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]; const supabase = createClient(url, key); async function test() { const playerId = '1a0b56fa-25ff-4b50-b9b1-0867f07d1b8b'; const tFilter = \player1_id.eq.\,player2_id.eq.\,player3_id.eq.\,player4_id.eq.\\; const { data: tournamentData } = await supabase.from('tournament_matches').select('*').in('status', ['completed', 'scheduled', 'in_progress', 'walkover']).or(tFilter).order('created_at', { ascending: false }); console.log('Tournaments count:', tournamentData.length); tournamentData.forEach(m => { let partnerId = null; if (m.player1_id === playerId && m.player3_id) partnerId = m.player3_id; else if (m.player3_id === playerId) partnerId = m.player1_id; else if (m.player2_id === playerId && m.player4_id) partnerId = m.player4_id; else if (m.player4_id === playerId) partnerId = m.player2_id; if (partnerId) console.log('Partner:', partnerId, 'in match', m.id, 'category:', m.category); }); } test();
