import fs from 'fs'; const env = fs.readFileSync('.env.local', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1].trim(); const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1].trim(); const get = async (table) => { const r = await fetch(url + '/rest/v1/' + table + '?select=*', { headers: { 'apikey': key, 'Authorization': 'Bearer ' + key }}); return r.json(); }; async function run() { const p = await get('players'); const raja = p.find(x => x.full_name.includes('Raja')); const abhi = p.find(x => x.full_name === 'Abhishek Sampath'); const tm = await get('tournament_matches'); const hm = tm.filter(m => ((m.player1_id === raja.id || m.player3_id === raja.id) && (m.player2_id === abhi.id || m.player4_id === abhi.id)) || ((m.player2_id === raja.id || m.player4_id === raja.id) && (m.player1_id === abhi.id || m.player3_id === abhi.id))); console.log('Tourney Matches with Abhishek Sampath:', hm.length); } run();
